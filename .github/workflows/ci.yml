name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
          components: clippy, rustfmt

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Format check
        run: cargo fmt --manifest-path contracts/milestone-escrow/Cargo.toml --check

      - name: Clippy
        run: cargo clippy --manifest-path contracts/milestone-escrow/Cargo.toml --all-targets -- -D warnings

      - name: Run tests
        run: cargo test --manifest-path contracts/milestone-escrow/Cargo.toml

      - name: Build WASM
        run: cargo build --release --target wasm32-unknown-unknown --manifest-path contracts/milestone-escrow/Cargo.toml
